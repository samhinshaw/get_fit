extends partials/layout

block header
  p.title
    | Get Fit!
  p.subtitle
    | Sam & Amelia are gonna be 
    strong sexy 
    | next summer! 👙 🏖

block subnav
  nav.navbar.has-shadow.is-light(id=pageInfo.user+'-subnav')
    .container
      .navbar-tabs
        a.navbar-item.is-tab.is-active(href='#')
          | Summary
        //- a.navbar-item.is-tab(href='#')
        //-   | Weight
        //- a.navbar-item.is-tab(href='#')
        //-   | Spend Points

block data
  table.table
    thead
      td
      td Sam
      td Amelia
    tr
      td Points
      td= userInfo.sam.points
      td= userInfo.amelia.points