extends partials/layout

block header
  p.title
    | Get Fit!
  p.subtitle
    | #{routeInfo.userName} & #{routeInfo.partnerName} are gonna be 
    strong sexy 
    | next summer! 👙 🏖

//- block subnav
  include includes/overview_subnav

block content
  .columns.is-desktop
    .column.is-2
    .column.is-4
      h3.is-size-3 #{userName}
      p Points: #{pointTally.sam.toFixed(1)}
    .column.is-4
      h3.is-size-3 #{partnerName}
      p Points: #{pointTally.amelia.toFixed(1)}
    .column.is-2
      