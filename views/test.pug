extends partials/layout

block header
  p.title
    | Get Fit!
  p.subtitle
    | #{routeInfo.userName} & #{routeInfo.partnerName} are gonna be 
    strong sexy 
    | next summer! 👙 🏖

block subnav
  include includes/test_subnav

block content
  .container
    .columns.is-fullhd
      .column.is-8.is-offset-2
        .field
          label.label Username:
          .control
            input.input#username(name='username', type='text', placeholder='jetknife')
        .field
          .control
            button.button#createUser Submit