extends partials/layout

block header
  p.title
    | Users
  p.subtitle
    | Login, register, manage your account 🤓

block subnav
  nav.navbar.has-shadow.is-light(id=pageInfo.user+'-subnav')
    .container
      .navbar-tabs
        a.navbar-item.is-tab.is-active(href='/account')
          | Overview
        if user
          a.navbar-item.is-tab(href='/account/logout') Logout
        else 
          a.navbar-item.is-tab(href='/account/login')
            | Login
          a.navbar-item.is-tab(href='/account/register')
            | Register


block content
  p Hello